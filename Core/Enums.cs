namespace SlimeDungeon.Core;

public enum Direction { Down, Up, Left, Right }

public enum Gender { Male, Female }

public enum WalkFrame { A, B }

public enum SlimeColor { Green, Red, Blue, Yellow, Gray, Poison, Gold, White }
